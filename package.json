{
  "name": "wechat-service-account",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "微信服务号内容预览、草稿和发布工具",
  "scripts": {
    "wechat": "node --import tsx src/cli.ts",
    "preview": "node --import tsx src/cli.ts preview --file content/example.md",
    "check": "tsc --noEmit",
    "test": "node --import tsx --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "commander": "^15.0.0",
    "dotenv": "^17.4.2",
    "gray-matter": "^4.0.3",
    "marked": "^18.0.6",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
